export default function StackTextBox({ children }: { children: any }) {
    return (
        <div className="flex flex-col justify-center items-center w-3/5">
            {children}
        </div>
    )
}