export default function ModalSubject({ subject }: { subject: string }) {
    return (
        <div className="ml-2 mr-6 mt-3 font-semibold text-sm md:text-lg">
            {subject}
        </div>
    )
}